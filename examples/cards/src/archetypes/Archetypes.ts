const enum Archetypes {
  Renderable,

  Cards,
  PlayerCards,
  OpponentCards,

  DeckCards,
  HandCards,
  PlayedCards,

  PlayerHandCards,
  PlayerDeckCards,
  PlayerPlayedCards,

  OpponentHandCards,
  OpponentDeckCards,
  OpponentPlayedCards
}

export default Archetypes

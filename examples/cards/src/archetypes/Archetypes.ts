const enum Archetypes {
  Renderable,

  Cards,
  DeckCards,
  HandCards,
  PlayedCards,

  PlayerHandCards,
  PlayerDeckCards,
  PlayerPlayedCards,

  OpponentHandCards,
  OpponentDeckCards,
  OpponentPlayedCards
}

export default Archetypes
